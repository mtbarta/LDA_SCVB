package LDA;

import java.io.*;
import java.util.*;
import java.util.concurrent.*;

public class DictVectorizer {
    private int numDocs;

    public DictVectorizer( FileStream x, int numDocs) 
    {
        this.numDocs = numDocs;
    }

    public run(ArrayList listFiles)
    {

        BufferedReader reader = new BufferedReader(new FileReader(file));


    }
}
